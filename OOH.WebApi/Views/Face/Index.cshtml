@{
    ViewData["Title"] = "Face";
}
<div>
    <div class="card">

        <div class="card-header">
            <button class="btn btn-primary" onclick="edit('0')"> <i class="fa fa-pencil-square-o"></i>Agregar Cara</button>
            <button class="btn btn-primary btn-outline-primary" onclick="refresh()"><i class="fa fa-refresh"></i>Actualizar</button>
        </div>
    </div>

    <div class="card">

        <div class="card-block">
            <table id="CarasTable" class="table table-hover">
                <thead>
                    <tr>
                        <th class="filter-action">Acciones</th>
                        <th class="filter">Codigo</th>
                        <th class="filter">Sitio</th>
                        <th class="filter">Tipo</th>
                        <th class="filter">Referencia</th>
                        <th class="filter">Municipio</th>
                        <th class="filter">Iluminada</th>
                    </tr>
                </thead>
                <tbody>
                </tbody>

            </table>


        </div>
    </div>
</div>

<script src="~/js/Face/jquery.face.js"></script>